{ pkgs, lib, ... }:
{
  config = {
    # Virtual display removed - Sunshine now captures directly from DP-1
    # DP-1 remains active even when TV is off, allowing streaming without virtual display
  };
}
